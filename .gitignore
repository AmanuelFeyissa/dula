# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
.env
.env.*
!.env.example
!.env.local.example
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
dist/
build/

# ---- uv ----
# lockfile IS committed (uv.lock); caches are not
.uv/

# ---- Node / TypeScript ----
node_modules/
.next/
out/
*.tsbuildinfo
.turbo/
.pnpm-store/
coverage/
playwright-report/
test-results/

# ---- Editors / OS ----
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.recommended.json
.DS_Store
Thumbs.db
*.swp

# ---- Secrets / local ----
*.pem
*.key
!**/testdata/**/*.key
secrets/
# Credentials must never be committed (keep them in the CLI's standard location instead).
.kaggle/
kaggle.json
*.hf_token
# ---- ML training artifacts (large; belong on HF Hub / DVC remote, not git) ----
ml/data/
out/
mlruns/
**/.ipynb_checkpoints/
*.local

# ---- Data / models / artifacts (tracked via DVC/registry, not git) ----
/data/
/models/
*.gguf
*.safetensors
*.bin
mlruns/

# ---- Docker / infra local state ----
.docker-data/
*.tfstate
*.tfstate.*
.terraform/
