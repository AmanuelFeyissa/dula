# Alembic config. The database URL is supplied by env.py from application settings
# (docs/07-Database/MigrationStrategy.md). Migrations are forward-only in production.
[alembic]
script_location = alembic
prepend_sys_path = .
