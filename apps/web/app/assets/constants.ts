// Shared between the list, detail, and create pages, and the server actions — one place to
// keep in sync with dula_platform_api.models.Criticality.
export const CRITICALITIES = ["critical", "high", "medium", "low"] as const;
